LANGUAGES += c
COMPILEOPTS += -Wall
COMPILEOPTS += -Werror
COMPILEOPTS += -pedantic
COMPILEOPTS += -g
COMPILEOPTS += -DDEBUG

BINARIES += pconfigure
LINKOPTS += -lclang
LINKOPTS += -lLLVM-2.8
LINKOPTS += -L`llvm-config --libdir`
LINKOPTS += -Wl,-R`llvm-config --libdir`
COMPILEOPTS += -I`llvm-config --includedir`
SOURCES += pconfigure/main.c


