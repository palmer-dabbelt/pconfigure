language: c++
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - build-essential
      - gcc-4.9
      - g++-4.9
before_install:
  - export CXX=g++-4.9
  - export CC=gcc-4.9
script:
  - ./bootstrap.sh
  - PATH="$(pwd)/bin" make check
